// SPDX-FileCopyrightText: Copyright (c) 2026 Quadrant contributors
// SPDX-License-Identifier: GPL-3.0-only

import { GalleryPage } from "../shared/gallery_page.slint";
import { NavigationViewSpecimen } from "../shared/navigation_view_specimen.slint";
import { NavigationDefaultsSpecimen } from "../shared/navigation_defaults_specimen.slint";
export component NavigationViewPage inherits GalleryPage {

    page_description: "Compose Back, Search, pane modes, one to three levels, destination groups, footer and content surfaces. Toggle each optional feature in the live configuration.";
    NavigationViewSpecimen {  }
    NavigationDefaultsSpecimen {  }
}
