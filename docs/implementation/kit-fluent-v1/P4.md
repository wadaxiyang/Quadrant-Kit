# P4 — completed native wrapper batches

All four ordered units are implemented and separately reported/committed:

| Batch | Delivery | Runtime | Paired release |
|---|---|---:|---|
| [P4A](P4A.md) | CheckBox, Switch, RadioGroup, ComboBox | 17 assertions | 30 selection-100 pairs |
| [P4B](P4B.md) | Slider, SpinBox, ProgressBar, ProgressRing | 17 assertions | 30 progress-100 pairs |
| [P4C](P4C.md) | ScrollView, ListView, StandardListView, GroupBox, TabWidget | 16 assertions | 30 lists-10000 pairs |
| [P4D](P4D.md) | StandardTableView, DatePicker, TimePicker, Date/Time types | 18 assertions | 30 table-100 pairs |

Scoped implementation/input gate: PASS. Current facade has 53 names / 37 visuals;
16 controls and two native data types were added since P3. All current callers,
API/probe/snapshot, Gallery/catalog and native coverage agree. Native RadioGroup,
ListView and TabWidget retain compiler identity through verified static exports.
No runtime registration, required adapter, private widget, frozen legacy consumer
or root runtime dependency was introduced. Existing native limitations are public.

P4D reconciles full core checks: fmt, Clippy, 20 Rust/75 Python tests, boundaries,
native reuse, Gallery build. Each batch has 40 closed-page Gallery captures and
30 release pairs; P4 reports retain exact source content, commands and raw paths.
The 68 runtime assertions and 160 Gallery captures are separate evidence categories.

Unchanged binary/provisional software-buffer/short memory delta allowances pass.
This does not certify actual presentation, long-idle CPU, all native keyboard,
reader/IME, real mixed-DPI monitor transitions, every locale or WinUI equivalence.
P3's missing actual WinUI reference remains open. Source-package followups are in
STATE; original root SPEC changes remain untouched. No external consumer change,
push or release. Proceed to P5A under the user's ordered authorization.
