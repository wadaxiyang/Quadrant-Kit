# P8 — current contracts and local candidate assessment

Status: **PARTIAL — scoped P7/P8 delivery complete, local candidate only; full
release acceptance remains limited.** P7 is committed as `8bd293d4647be70489a08f812a532d6cccd4b73e`. P8 contains
documentation reconciliation and final verification; it changes no UI, public
signature, dependency, backend or external Tasks revision. This document describes
the P8 working tree at the P7 parent plus dirty documentation; its final commit
is the Git commit containing this report, not a guessed self-referential SHA.

## Current contract and documentation closure

The facade, reviewed current JSON, probe, PUBLIC_API declarations, active callers,
native reuse manifest and Gallery catalog agree: 59 names = 42 visual components,
six globals, eight enums and three structs; 290 explicit properties, 34 callbacks
and nine public functions. Inherited native members are additionally checked by
the pinned Slint compiler. Every visual component has a typed reachable specimen;
the catalog has 32 destinations (39 entries including groups). The native manifest
has 44 records, including two private helpers, and six narrowly scoped exceptions.
No snapshot refresh, compatibility alias or historical consumer fixture was added.

README, ARCHITECTURE, GALLERY, VALIDATION, NATIVE_REUSE, COMPONENT_STATUS,
CONSUMER_GUIDE and performance/motion references now distinguish the historical
extraction/navigation rebuild from the current Fluent contracts. Historical
reports, retained SHA examples and PASS/FAIL/NOT_RUN results are not rewritten.
README retains editable Mermaid source and the static-export explanation; its
dependency direction agrees with code and the boundary guard. Mermaid renderer
execution is NOT_RUN. Root/UI/Gallery AGENTS already enforce the current version
policy and layer ownership; no permanent rules were changed to hide phase gaps.

Current breaking changes remain in PUBLIC_API and CHANGELOG: removed command
preview/old appearance inputs, native command activation and controlled selection,
new native selection/numeric/container/table/picker/popups, reactive bounded
navigation, finite confirmation focus-restore requests, host-conditional Expander,
and per-window reduced/disabled Kit motion. P7/P8 add no further API break.
New consumers import `@quadrant-kit` through the build-helper mapping, embed static
resources and initialize Theme/Palette/font/system state in every top-level
window. Hosts still own business state, routing, event loops and OS actions.
Developers needing an older API select its retained version; current source has
one implementation per version, without old adapters.

## Distribution and independent integration

At clean P7 `8bd293d4647be70489a08f812a532d6cccd4b73e`, all seven ordered
post-commit commands pass (`target/p7-postcommit/result.json`): source distribution,
Cargo package list, actual archive consumer, Windows Rust 1.92 helper/Gallery build,
isolated clone creation, incremental token/SVG verification and clean clone status.
The archive consumer's full report is
`target/package-consumer/20260910T031029372291Z/result.json`; archive SHA-256
`cca78c667162b16c81b2aaec662798df1fe0e4751706bcf3003451e6ebad48c7`.
It resolves Kit only from the extracted archive, compiles the current complete API
probe, then runs a copied executable from an empty directory in Light and Dark.
Both images were visually inspected: title, icon, command and Toast render in both
themes. This uses an existing dependency/build cache, not a fresh remote Git consumer.

Incremental evidence is copied to `target/p7-postcommit/incremental/`: both deep
Theme token and SVG edits rerun the build script and change the binary; original
file bytes are restored and `git status --porcelain` in the isolated clone is empty.
The Windows target was exclusive to that clone for the run. Linux uses a separate
root checkout/build directory. Before the final Windows gate, the small helper
cache was rebuilt to avoid retaining a clone-specific compile-time facade path.
P8 changes documentation only; implementation and asset bytes remain the same.
Final clean-P8 package results are retained separately in `target/p8-postcommit/`
after the commit; they do not replace the identified P7 archive evidence above.

The static resource closure is 87 required files including notices and 32 local
MIT SVGs, verified against their hashes and per-asset attribution. Required source
and license bytes are compared against the actual .crate; no Gallery binaries,
screenshots, measurements, target cache, absolute machine settings or system fonts
are shipped. All registered SVGs are reachable; static reachability through an
icon global is not proof that every icon is used by every consumer. Measured
empty/import-only binaries and required resource attribution are in P7.

## Visual and platform evidence

The current Windows Catalog capture produces 128 fresh PNG/JSON pairs:
32 destinations × Light/Dark × 1040x800/760x520, at 100% scale, winit/software.
All processes and nonempty-image checks pass. Raw directory:
`target/verify-p8/visual/dirty-f134adbd19dbda68df52fa1620ec871e2036250246404ce1f00eeec5b4aa7f63/windows-winit-software/`.
Source is the P7 HEAD plus P8 documentation changes; every image records exact
source content and binary hash. The code/API/resources are unchanged by P8.
Manual inspection covers the representative images listed in
`target/p8-visual-review.json`; image generation success is kept separate from
the visual findings there. It is not manual review of every image or a WinUI
reference-image comparison. The six reviewed pages are FluentButton, ModalManager, NavigationView, native selection, native popups and native inline. Their initial body viewports pass the scoped layout review; narrow pages still require scrolling, and live modal/popups are closed in the catalog snapshots. Windows Chinese labels are visible. The Windows header region appears black/washed out in these software captures; P8 does not establish actual DWM caption/toolbar appearance or full-window visual acceptance.

Native controls follow pinned Slint Fluent rendering rather than promising WinUI
3 pixel identity. Earlier reports retain native limits on forced narrow labels,
RadioGroup grammar/arrows, static native tabs, fixed model bounds, popup positioning
and full dialog/reader behavior. The current shell is unchanged. Software snapshots
do not capture OS-composited caption controls; simulated scale does not certify
moving between physical monitors.

| Platform / check | Result and actual scope |
|---|---|
| Windows x86_64 MSVC / Rust 1.94.1 | Full final core gate PASS; current Gallery Catalog software captures. P7 input/UIA observations remain separately scoped. |
| Windows Rust 1.92.0 | Actual helper + Gallery build PASS, `target/p7-postcommit/3.log`; not inferred from rust-version metadata. |
| Ubuntu 24.04 x86_64 under WSL2 / Rust 1.94.1 | fmt/Clippy, 19 Rust tests, boundaries and Gallery build PASS. Python discovers 88 tests: 87 pass, one PowerShell 7 wrapper test is skipped because pwsh is unavailable. |
| Linux WSLg software runtime | Native-selection 1040x800 Light/100% smoke executes and produces a nonempty image; manual Latin/control layout inspection passes. Chinese glyph visual check FAIL in this environment: Chinese text after the slash is absent; `fc-list :lang=zh family` is empty. Full Linux visual/font acceptance remains PARTIAL. |
| macOS | Build/native runtime/input/reader NOT_RUN; no macOS host was used. |
| Remote CI / retained current Git consumer | NOT_RUN for the local P7/P8 commits. |

Linux evidence is under `target/p7-linux/`. Its first Clippy attempt failed with
`Kit facade is missing` after reuse of a cache from the previous checkout location.
That failure is retained in `clippy-first-failure.log`. Scoped cleaning of only
the Linux Kit/Gallery packages and rebuilding succeeds without any source change.
Native libraries use the existing validation-only user sysroot, scoped through
PKG_CONFIG/LD_LIBRARY_PATH; no repository linker override or font file was copied.
The Linux image records P7 HEAD plus the in-progress P8 documentation content hash
`f134adbd19dbda68df52fa1620ec871e2036250246404ce1f00eeec5b4aa7f63`.
Host font provisioning and a repeated Chinese rendering check remain platform
follow-up work, not a reason to bake system fonts into Kit.

## Final verification and remaining gates

`target/verify-p8/result.json` passes all seven final Windows checks: fmt,
full workspace/all-targets/all-features Clippy with denied warnings, 20 Rust tests,
current boundary/API/distribution closure, native reuse, 88 Python tests and Gallery
build. Tested parent is `8bd293d4647be70489a08f812a532d6cccd4b73e`, dirty=true,
content `f134adbd19dbda68df52fa1620ec871e2036250246404ce1f00eeec5b4aa7f63`. Final report/result text is updated
after these runs; implementation/API/asset inputs stay unchanged. The final local
commit is identified by Git and the task result, without fabricating a self SHA.

P7 retains 1,260 startup processes across 21 scenarios, 7,200 interaction frames,
257 fresh runtime assertions, Windows native menu input and UIA Invoke/focus/
disabled evidence, three Gallery round-trip runs plus one page-timer probe, and
long-idle observations. Nineteen original matched startup scenes and the additional
matched hidden-Toast composition pass their named software/memory/binary budgets;
the unmatched empty/Toast row remains separately BLOCKED with its raw FAIL intact.
All six interaction snapshot and total-dispatch-plus-snapshot gates pass. No budget
was changed and no control behavior was removed to reduce cost.

The following remain distinct from the passing local checks:

- Actual screen presentation/frame timing, cold-start cache isolation, GPU memory,
  exact sustained redraw counts, OS IME composition and screen-reader sessions:
  NOT_RUN. UIA is a narrower real Windows accessibility observation.
- Gallery long-term memory stability: unresolved. The probe navigates forward back to home, so Gallery-owned Back history intentionally retains 200 entries; that small known host allocation alone does not explain all observed process-memory differences. Measured page-root activity is
  bounded, but retained private-memory fluctuations/late growth are not diagnosed
  as allocator caching or leaks. The raw trend remains in P7; no leak-free claim.
- Arbitrary-content ContentDialog containment, nested modal stacks and complete
  screen-reader semantics: outside the delivered finite confirmation contract.
- Expander slot teardown requires host-conditional construction. Navigation permits
  at most 256 entries across primary/footer models. Native-owned motion is not
  universally disabled by Kit's policy.
- Full physical monitor/DPI changes and broader platform input/reader coverage:
  NOT_RUN. A build or software snapshot does not fill those categories.
- Same-SHA remote CI, retained publication reference and fresh anonymous Git+SHA
  consumer for these local commits: NOT_RUN. Historical extraction evidence remains
  valid only for its own source. No push, tag, release or Tasks cutover was performed.

These limits prevent describing the local candidate as universally release-verified.
The requested phase work is recorded and locally committed in order; any future
publication or external consumer adoption requires its own explicit authorization
and remaining evidence. No full WinUI 3 runtime or old-source compatibility promise
is made.
