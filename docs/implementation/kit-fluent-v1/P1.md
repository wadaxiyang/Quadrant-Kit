# P1 — modular boundaries, current-version policy and native reuse guard

Date: 2026-09-09. Requested phase: **P1 only**, explicitly authorized after P0.

## Identity and scope

- Before/after HEAD: `737d0aae0975232f520cc1e82640d99e8eb49467`, branch `main`.
  No commit, push, publication, tag, checkout or external consumer update.
- P0 changes remain uncommitted. The user's deleted
  `Quadrant_Kit_NavigationView_Gallery_Rebuild_SPEC.md` and untracked
  `Quadrant_Kit_Fluent_Evolution_SPEC.md` are preserved.
- Final executable checks used HEAD plus dirty content digest
  `72e3a305e206c68a9e24ed87a0c3780e027fadc6df5214a852d9b99b9a617370`.
  The digest was identical before/after the complete run. It is not a Git revision.
  This report, final ledger and documentation authority/source annotations were
  written afterward; no executable source changed after the run.
- Production Slint components/facade, root helper, Cargo manifests/lock/toolchain,
  assets, current API baseline and Gallery runtime/controller remain unchanged.
  Gallery build.rs/native verification facilities originated in P0; P1 extends
  the existing API probe and native probe only. Normal startup instantiates neither.

## Delivered work and review

1. Root, UI and Gallery AGENTS incorporate SPEC section 15's lasting rules, including
   static facade entry, current-version evolution, native ownership, host boundaries,
   single writer, scoped stages and publication authorization. README contains the
   exact editable section 3.5 Mermaid and static integration explanation. ARCHITECTURE
   has the ownership/dependency table. The diagram describes allowed edges; foundation
   does not yet use its permitted public Palette design-output edge.
2. Current API/docs/probe tests compare current artifacts instead of requiring fixed
   historical counts. No legacy consumer fixture or compatibility implementation was
   added. Existing valid navigation behavior assertions remain. The API probe adds
   controlled navigation/selection and host assignments, explicit enabled bindings for
   slot children, child content, public focus calls and narrow editor dimensions.
3. The manifest inventories 22 implementations, including 21 public visual components
   and private NavigationItemRow. Results: two native wrappers, four presenters,
   seven compositions, four reviewed exceptions and five pending migrations.
   **FluentButton, IconButton, SegmentButton, WindowControlButton and NavigationItemRow
   remain custom/pending-migration, not compliant.**
4. The new native guard reuses the existing fail-closed scanner. It checks full record
   coverage, import aliases/actual native ownership, composition, duplicate input,
   literal hidden proxies, foundation instances and canonical reviewed custom bodies.
   It is invoked by the existing boundary command, so existing CI paths include it.
   New pending command debt fails. Each allowance must be removed when migrated.
   Exception justification and dynamic/runtime behavior still need human review/tests.
5. Scanner fixtures cover verified public Date/Time re-exports and builtin table/model
   type references. Unknown std re-exports remain errors. The locked compiler exposed
   that local `export { Alias }` forbids a trailing semicolon, unlike `from` exports;
   scanner, fixtures and native probe now agree. No native type entered Kit's facade.
6. COMPONENT_STATUS defines current implementation/verification gaps, reachable Gallery
   specimens, planned Fluent names and independently gated P4/P5 batches. PUBLIC_API,
   CONSUMER_GUIDE, GALLERY and VALIDATION document synchronized current callers, reviewed
   signature/default changes, state ownership and removal of replaced implementations.
7. PERFORMANCE freezes the matched simple-scene binary increment at
   `max(512KiB, 5% of A)` and keeps hidden-work/input/accessibility constraints.
   Startup, steady memory, frames and idle CPU retain SPEC thresholds with explicit
   measurement prerequisites. P0's three-pair smoke and unsupported rendering hook do
   not justify quantitative startup acceptance. The hidden-Toast 2.159MiB increment
   versus empty remains unaccepted; no budget was increased to conceal it.

API review: **no additions, removals, renames, type/direction/base/default changes,
state-ownership changes or facade changes**. The generated candidate is byte-identical
to the reviewed snapshot. Current exports, PUBLIC_API declarations and probe uses
match. No snapshot was adopted/refreshed.

## Executed checks

Exact commands, exit codes and durations are retained in
[final/checks.json](../../../target/kit-fluent-p1/final/checks.json).
Log names below are under [final/](../../../target/kit-fluent-p1/final/).

| Command/scenario | Result | Exit | Evidence / scope |
|---|---|---|---|
| `cargo fmt --all --check` | PASS | 0 | fmt.log |
| `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | PASS | 0 | clippy.log |
| `cargo test --workspace --locked` | PASS | 0 | rust-tests.log; 1 Kit + 19 Gallery tests |
| `python scripts/check_ui_boundaries.py` | PASS | 0 | boundaries.log; current API/layers/Cargo/assets plus native guard |
| `python scripts/check_native_reuse.py` | PASS | 0 | native-reuse.log; pending debt printed, runtime_verified=false |
| `python scripts/check_native_reuse.py --help` | PASS | 0 | native-help.log |
| `python -m unittest discover -s scripts/tests -p "test_*.py"` | PASS | 0 | python-tests.log; 66 tests, no skips |
| `cargo build --locked -p quadrant-kit-gallery` | PASS | 0 | build.log; current API and native probes compiled |
| `python scripts/run_perf.py --native-probe` | PASS — compilation | 0 | native-capabilities.log; one positive build and three expected rejections |
| `python scripts/check_ui_boundaries.py --write-baseline target/kit-fluent-p1/final/api-candidate.json` | PASS — generation only | 0 | api-candidate.log; no baseline adopted |
| `git diff --no-index scripts/kit_api_v1.json target/kit-fluent-p1/final/api-candidate.json` | PASS — no diff | 0 | api-diff.log |
| `python scripts/verify_distribution.py --package` | **FAIL** | 1 | distribution.log; nested package list rejects uncommitted source |
| `cargo package --locked -p quadrant-kit --list` | **FAIL** | 101 | package-list.log; clean-source prerequisite unmet |
| `python scripts/verify_distribution.py` | PASS — static closure | 0 | static-distribution.log; 65 static files, 32 SVGs; package_checked=false |
| Final `git diff --check`, production/API/resource diff and local Markdown link review | PASS | 0 | Working-tree review; unchanged production/API/resource paths, new report/ledger links resolve |

The independent native report is
[20260909T080019737704Z/result.json](../../../target/perf-harness/20260909T080019737704Z/result.json).
It retains the positive exit 0 and three intended exit 101 builds with required
diagnostics for Palette writes, RadioGroup.current-index and ListView child slots.
This run has no runtime/performance samples and does not certify native interaction.

Initial failures remain in [checks.json](../../../target/kit-fluent-p1/checks.json)
and its adjacent logs: Clippy/tests/build/native probe rejected the extra local
re-export semicolon. That source defect was fixed, then the full run above passed
its code checks. Initial package failures remain failures; no baseline refresh,
allow-dirty bypass or auto-commit was used.

## Unrun work and acceptance limits

| Check/dimension | Status | Reason / next condition |
|---|---|---|
| Distribution archive build/verification | NOT_RUN; **BLOCKED** | Both package prerequisites actually failed on uncommitted source; needs separately authorized clean candidate |
| MSRV 1.92 build | NOT_RUN | No Rust/toolchain/dependency changes in P1; historical evidence is not a rerun |
| `python scripts/verify_incremental.py` | NOT_RUN | Mutates/restores token and SVG; no production source/resource change requiring this exclusive invalidation run |
| New release performance/idle/lifetime/frames | NOT_RUN | P1 defines protocol from P0 evidence; measurement obligations remain in PERFORMANCE |
| Real input, IME, focus restoration and screen reader | NOT_RUN | P1 probe is compile-only; scoped runtime work remains in later stages |
| Gallery screenshots/native chrome and WinUI comparison | NOT_RUN | No production appearance change; no visual acceptance inferred |
| Mermaid rendering in a Markdown host | NOT_RUN | Editable source equality and allowed layer graph checked; no rendered screenshot claim |
| Linux/macOS, remote CI and public Git+SHA consumer | NOT_RUN | No platform/publication/consumer run in P1 |

P1's scoped policy/scanner/probe gate is **PASS**. Full common-gate/distribution
acceptance remains **BLOCKED**, not PASS. Static reuse checks are not overall native
compliance. P2–P8 remain NOT_STARTED. Stop here; the next explicit phase would be P2's
FluentButton vertical slice, not all remaining button or new-control migrations.
